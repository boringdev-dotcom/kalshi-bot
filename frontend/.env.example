# Backend API URL (defaults to /api for same-origin)
# For cross-origin, use full URL: http://localhost:8000/api
VITE_API_URL=http://localhost:8000/api

# Note: The Odds API key is now configured on the backend (ODDS_API_KEY)
# See the root .env.example for backend configuration
